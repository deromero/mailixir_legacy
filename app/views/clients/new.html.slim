h1 New client

- if @client.errors.any?
  .error
    h2
      = pluralize(@campaign.errors.count, "error")
      | prohibited this client form being saved
    ul
      - @campaign.errors.full_messages.each do |msg|
        li = msg

= form_for @client do |f|
  = f.label :name
  = f.text_field :name

  = f.label :description
  = f.text_area :description

  = f.submit "Save"
